{% extends 'adminbase.html.twig' %}

{% block title %}Edit Companies{% endblock %}

{% block body %}
<div class="main-panel">
		<div class="content-wrapper">
    <h1>Edit Companies</h1>

    {{ include('admin/companies/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_companies_index') }}" class="btn btn-primary btn-fw">Back to List</a>
    </br>
 </br>
    {{ include('admin/companies/_delete_form.html.twig') }}
     </div>

{% endblock %}
