{% extends 'base.html.twig' %}

{% block title %}CarImage{% endblock %}

{% block body %}
    <h1>CarImage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car_image.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ car_image.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ car_image.image }}</td>
            </tr>
            <tr>
                <th>Carid</th>
                <td>{{ car_image.carid }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_car_image_index') }}">back to list</a>

    <a href="{{ path('admin_car_image_edit', {'id': car_image.id}) }}">edit</a>

    {{ include('admin/car_image/_delete_form.html.twig') }}
{% endblock %}
